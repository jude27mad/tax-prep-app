[pytest]
addopts =
    -p pytest_playwright
    -p no:anyio
    --strict-markers
markers =
    smoke: high-level smoke tests covering the primary filing workflows.
    playwright_smoke: Playwright-powered smoke coverage for the upload→apply→preview UI flow.
asyncio_mode = strict
