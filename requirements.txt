fastapi>=0.117,<1.0
# python-multipart: >=0.0.22 fixes GHSA-wp53 (Arbitrary File Write), >=0.0.26 fixes GHSA-mj87 (DoS via large preamble/epilogue),
# >=0.0.27 fixes GHSA-pp6c-gr5w-3c5g / CVE-2026-42561 (DoS via unbounded multipart part headers).
python-multipart>=0.0.27,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.11,<3.0
httpx>=0.27,<1.0
anyio>=4.4,<5.0
# python-dotenv: >=1.2.2 fixes GHSA-mf9w (Symlink following in set_key).
python-dotenv>=1.2.2,<2.0
reportlab>=4.2,<5.0
# pytest: >=9.0.3 fixes GHSA-6w46 (vulnerable tmpdir handling). Major-version bump from 8.x.
pytest>=9.0.3,<10.0
pytest-asyncio>=1.3.0,<2.0
ruff>=0.6,<1.0
mypy>=1.11,<2.0
xmlschema>=1.11,<5.0
# cryptography: >=46.0.5 fixes GHSA-r6ph (SECT curves subgroup attack), >=46.0.6 fixes GHSA-m959 (DNS name constraint). Major bump from 43.x.
cryptography>=46.0.6,<47.0
hypothesis>=6.112,<7.0
jinja2>=3.1,<4.0
rich>=13.7,<14.0
pytesseract>=0.3.10,<0.4
pdf2image>=1.17,<2.0
# pillow: >=12.1.1 fixes GHSA-cfh3 (OOB write on PSD), >=12.2.0 fixes GHSA-whj4 (FITS GZIP decompression bomb). Major bump from 10.x.
pillow>=12.2.0,<13.0
playwright>=1.47,<2.0
pytest-playwright>=0.7.2,<1.0

# D1.3: persistent document vault — SQLModel on SQLAlchemy 2.0 async + SQLite.
sqlmodel>=0.0.24,<1.0
sqlalchemy>=2.0,<3.0
aiosqlite>=0.20,<1.0
alembic>=1.14,<2.0

# D1.4: magic-link auth — signed session cookies via starlette.middleware.sessions.
itsdangerous>=2.2,<3.0

# D1.7: SMTP transport for magic-link emails. Async submission so the
# auth request handler doesn't block the event loop on slow MTAs.
aiosmtplib>=3.0,<4.0

# Transitive dependencies pinned explicitly to pull in security fixes.
# urllib3: >=2.6.3 fixes GHSA-gm62 (unbounded link chain), GHSA-2xpw (streaming decompression), GHSA-38jv (decompression-bomb on redirect);
# >=2.7.0 fixes GHSA-mf9v-mfxr-j63j / CVE-2026-44432 (streaming-API decompression bomb) and GHSA-qccp-gfcp-xxvc (sensitive headers on cross-host proxy redirect).
urllib3>=2.7.0,<3.0
# starlette: >=0.49.1 fixes GHSA-7f5h (O(n^2) DoS via Range header); >=1.0.1 fixes
# GHSA-86qp-5c8j-p5mr / CVE-2026-48710 (Host-header poisoning of request.url.path).
# fastapi (>=0.115) declares starlette>=0.46.0 with no upper bound, so 1.x is allowed.
starlette>=1.0.1,<2.0
# requests: >=2.33.0 fixes GHSA-gc5v (insecure temp file reuse in extract_zipped_paths).
requests>=2.33.0,<3.0
# Pygments: >=2.20.0 fixes GHSA-5239 (ReDoS on GUID regex). Transitive via rich.
Pygments>=2.20.0,<3.0
# Mako: >=1.3.12 fixes GHSA-2h4p-vjrc-8xpq / CVE-2026-44307 (Windows backslash path traversal in TemplateLookup). Transitive via alembic.
Mako>=1.3.12,<2.0
# idna: >=3.15 fixes GHSA-65pc-fj4g-8rjx / CVE-2026-45409 (DoS bypass of the CVE-2024-3651 fix in idna.encode). Transitive via requests/httpx/anyio.
idna>=3.15,<4.0
